// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

export const firebaseConfig = {
  apiKey: 'AIzaSyDPQ_5G1534NoHjBzfjztab6r9OhTLoAdk',
  authDomain: 'kla-pointing-poker.firebaseapp.com',
  databaseURL: 'https://kla-pointing-poker.firebaseio.com',
  projectId: 'kla-pointing-poker',
  storageBucket: 'kla-pointing-poker.appspot.com',
  messagingSenderId: '901715127063'
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
